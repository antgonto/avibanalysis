package example;

public class Methods {
   
  // Constructor method
 
  Methods() {
    System.out.println("Constructor method is called when an object of it's class is created");
	//START @..CLON:0@..COPY:0@..ID_GENERAL:1
	final double SQRT = Math.sqrt(num);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	int resultad = (base10Num % 2) + resultad;
	final double SQRT = Math.sqrt(num);
	boolean result;
	if (other == null)
		result = false;
	else if (this == other)
		result = true;
	list1 = new IntListVer2();
	for (int i = 0; i < 10000; i++)
		list1.add(i);
	s.start();
	list1.toString();
	s.stop();
	assert data != null && data.length > 0:"Failed precondition";
	name = data[0];
	partners = new ArrayList<String>();
	for (int i = 1; i < data.length; i++)
		partners.add(data[i]);
	return true;
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	if (num % 58 == 0)
		result++;
	//END @..CLON:0@..COPY:0@..ID_GENERAL:1
	//START @..CLON:0@..COPY:2@..ID_GENERAL:3
	final double SQRT = Math.sqrt(num);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	int resultad = (base10Num % 2) + resultad;
	final double SQRT = Math.sqrt(num);
	boolean result;
	if (other == null)
		result = false;
	else if (this == other)
		result = true;
	list1 = new IntListVer2();
	for (int i = 0; i < 10000; i++)
		list1.add(i);
	s.start();
	list1.toString();
	s.stop();
	assert data != null && data.length > 0:"Failed precondition";
	name = data[0];
	partners = new ArrayList<String>();
	for (int i = 1; i < data.length; i++)
		partners.add(data[i]);
	return true;
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	if (num % 58 == 0)
		result++;
	//END @..CLON:0@..COPY:2@..ID_GENERAL:3
	//START @..CLON:1@..COPY:0@..ID_GENERAL:5
	boolean ascending = true;
	int index = 1;
	while (ascending && index < list.length){assert index >= 0 && index < list.length;ascending=(list[index - 1] <= list[index]);index++;}
	String s1 = "Computer Science";
	int x = 307;
	String s2 = s1 + " " + x;
	PrintStream writer = new PrintStream(new File("randInts.txt"));
	Random r = new Random();
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	String text = new String(
			Files.readAllBytes(Paths
					.get("C:\\Users\\Steven\\Documents\\GitHub\\avibanalysis\\Code-Cloner Java\\Cloner\\src\\Config.js")),
			StandardCharsets.UTF_8);
	JSONObject obj = new JSONObject(text);
	final String project = obj.getString("path");
	final String pathSalida = obj.getString("salida");
	int minLimit = obj.getInt("minLimit");
	base10Num = Math.abs(base10);
	base10Num = Math.abs(base10);
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	if (num % 58 == 0)
		result++;
	//END @..CLON:1@..COPY:0@..ID_GENERAL:5
	//START @..CLON:1@..COPY:1@..ID_GENERAL:6
	boolean ascending = true;
	int index = 1;
	while (ascending && index < list.length){assert index >= 0 && index < list.length;ascending=(list[index - 1] <= list[index]);index++;}
	String s1 = "Computer Science";
	int x = 307;
	String s2 = s1 + " " + x;
	PrintStream writer = new PrintStream(new File("randInts.txt"));
	Random r = new Random();
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	String text = new String(
			Files.readAllBytes(Paths
					.get("C:\\Users\\Steven\\Documents\\GitHub\\avibanalysis\\Code-Cloner Java\\Cloner\\src\\Config.js")),
			StandardCharsets.UTF_8);
	JSONObject obj = new JSONObject(text);
	final String project = obj.getString("path");
	final String pathSalida = obj.getString("salida");
	int minLimit = obj.getInt("minLimit");
	base10Num = Math.abs(base10);
	base10Num = Math.abs(base10);
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	if (num % 58 == 0)
		result++;
	//END @..CLON:1@..COPY:1@..ID_GENERAL:6
  }
 
  // Main method where program execution begins
 
  public static void main(String[] args) {
    staticMethod();
    Methods object = new Methods();
    object.nonStaticMethod();
	//START @..CLON:0@..COPY:3@..ID_GENERAL:4
	final double SQRT = Math.sqrt(num);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	int resultad = (base10Num % 2) + resultad;
	final double SQRT = Math.sqrt(num);
	boolean result;
	if (other == null)
		result = false;
	else if (this == other)
		result = true;
	list1 = new IntListVer2();
	for (int i = 0; i < 10000; i++)
		list1.add(i);
	s.start();
	list1.toString();
	s.stop();
	assert data != null && data.length > 0:"Failed precondition";
	name = data[0];
	partners = new ArrayList<String>();
	for (int i = 1; i < data.length; i++)
		partners.add(data[i]);
	return true;
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	if (num % 58 == 0)
		result++;
	//END @..CLON:0@..COPY:3@..ID_GENERAL:4
	//START @..CLON:1@..COPY:2@..ID_GENERAL:7
	boolean ascending = true;
	int index = 1;
	while (ascending && index < list.length){assert index >= 0 && index < list.length;ascending=(list[index - 1] <= list[index]);index++;}
	String s1 = "Computer Science";
	int x = 307;
	String s2 = s1 + " " + x;
	PrintStream writer = new PrintStream(new File("randInts.txt"));
	Random r = new Random();
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	String text = new String(
			Files.readAllBytes(Paths
					.get("C:\\Users\\Steven\\Documents\\GitHub\\avibanalysis\\Code-Cloner Java\\Cloner\\src\\Config.js")),
			StandardCharsets.UTF_8);
	JSONObject obj = new JSONObject(text);
	final String project = obj.getString("path");
	final String pathSalida = obj.getString("salida");
	int minLimit = obj.getInt("minLimit");
	base10Num = Math.abs(base10);
	base10Num = Math.abs(base10);
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	if (num % 58 == 0)
		result++;
	//END @..CLON:1@..COPY:2@..ID_GENERAL:7
  }

  static void staticMethod1() {
    System.out.println("Static method can be called without creating object");
	//START @..CLON:0@..COPY:1@..ID_GENERAL:2
	final double SQRT = Math.sqrt(num);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	int resultad = (base10Num % 2) + resultad;
	final double SQRT = Math.sqrt(num);
	boolean result;
	if (other == null)
		result = false;
	else if (this == other)
		result = true;
	list1 = new IntListVer2();
	for (int i = 0; i < 10000; i++)
		list1.add(i);
	s.start();
	list1.toString();
	s.stop();
	assert data != null && data.length > 0:"Failed precondition";
	name = data[0];
	partners = new ArrayList<String>();
	for (int i = 1; i < data.length; i++)
		partners.add(data[i]);
	return true;
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	if (num % 58 == 0)
		result++;
	//END @..CLON:0@..COPY:1@..ID_GENERAL:2
  }
   static void staticMethod2() {
    System.out.println("Static method can be called without creating object");
  } 

  static void staticMethod3() {
    System.out.println("Static method can be called without creating object");
  } 

  static void staticMethod4() {
    System.out.println("Static method can be called without creating object");
	//START @..CLON:2@..COPY:2@..ID_GENERAL:11
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	base10Num = Math.abs(base10);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	base10Num = Math.abs(base10);
	System.out.println(new BigInteger(1024, 10, new Random()));
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int result = 0;
	if (n == 0)
		result = 1;
	else
		result = n * fact(n - 1);
	String result = "size: " + iSize + ", elements: [";
	for (int i = 0; i < iSize - 1; i++)
		result += iValues[i] + ", ";
	if (iSize > 0)
		result += iValues[iSize - 1];
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int resultad = (base10Num % 2) + resultad;
	if (num % 58 == 0)
		result++;
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	base10Num = Math.abs(base10);
	//END @..CLON:2@..COPY:2@..ID_GENERAL:11
  } 

  static void staticMethod5() {
    System.out.println("Static method can be called without creating object");
  } 

  static void staticMethod6() {
    System.out.println("Static method can be called without creating object");
	//START @..CLON:1@..COPY:3@..ID_GENERAL:8
	boolean ascending = true;
	int index = 1;
	while (ascending && index < list.length){assert index >= 0 && index < list.length;ascending=(list[index - 1] <= list[index]);index++;}
	String s1 = "Computer Science";
	int x = 307;
	String s2 = s1 + " " + x;
	PrintStream writer = new PrintStream(new File("randInts.txt"));
	Random r = new Random();
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	int pos = 0;
	while (pos < size() && value > get(pos)) {
		pos++;
	}
	String text = new String(
			Files.readAllBytes(Paths
					.get("C:\\Users\\Steven\\Documents\\GitHub\\avibanalysis\\Code-Cloner Java\\Cloner\\src\\Config.js")),
			StandardCharsets.UTF_8);
	JSONObject obj = new JSONObject(text);
	final String project = obj.getString("path");
	final String pathSalida = obj.getString("salida");
	int minLimit = obj.getInt("minLimit");
	base10Num = Math.abs(base10);
	base10Num = Math.abs(base10);
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	if (num % 58 == 0)
		result++;
	//END @..CLON:1@..COPY:3@..ID_GENERAL:8
	//START @..CLON:2@..COPY:0@..ID_GENERAL:9
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	base10Num = Math.abs(base10);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	base10Num = Math.abs(base10);
	System.out.println(new BigInteger(1024, 10, new Random()));
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int result = 0;
	if (n == 0)
		result = 1;
	else
		result = n * fact(n - 1);
	String result = "size: " + iSize + ", elements: [";
	for (int i = 0; i < iSize - 1; i++)
		result += iValues[i] + ", ";
	if (iSize > 0)
		result += iValues[iSize - 1];
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int resultad = (base10Num % 2) + resultad;
	if (num % 58 == 0)
		result++;
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	base10Num = Math.abs(base10);
	//END @..CLON:2@..COPY:0@..ID_GENERAL:9
  } 

  static void staticMethod7() {
    System.out.println("Static method can be called without creating object");
  }
 
  // Non static method
 
  void nonStaticMethod() {
    System.out.println("Non static method must be called by creating an object");
	//START @..CLON:2@..COPY:1@..ID_GENERAL:10
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	startTime = System.nanoTime();
	stopTime = System.nanoTime();
	base10Num = Math.abs(base10);
	lineFromFile = scannerToReadAirlines.nextLine();
	airlineNames = lineFromFile.split(",");
	newAirline = new Airline(airlineNames);
	String goal = keyboard.nextLine();
	airlinesPartnersNetwork.add(newAirline);
	base10Num = Math.abs(base10);
	System.out.println(new BigInteger(1024, 10, new Random()));
	char[][] board = blankBoard(n);
	boolean solved = canSolve(board, 0);
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int result = 0;
	if (n == 0)
		result = 1;
	else
		result = n * fact(n - 1);
	String result = "size: " + iSize + ", elements: [";
	for (int i = 0; i < iSize - 1; i++)
		result += iValues[i] + ", ";
	if (iSize > 0)
		result += iValues[iSize - 1];
	int temp;
	boolean changed = true;
	for (int i=0;i < list.length && changed;i++){changed=false;for (int j=0;j < list.length - i-1;j++){assert (j > 0) && (j + 1 < list.length):"loop counter j " + j+"is out of bounds.";if (list[j] > list[j + 1]){changed=true;temp=list[j + 1];list[j + 1]=list[j];list[j]=temp;}}}
	int resultad = (base10Num % 2) + resultad;
	if (num % 58 == 0)
		result++;
	for (int i = 2; i <= 100; i++) {
		resultado *= i;
	}
	base10Num = Math.abs(base10);
	//END @..CLON:2@..COPY:1@..ID_GENERAL:10
  }
}